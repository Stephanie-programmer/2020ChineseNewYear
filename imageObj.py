class ImageObj:
    rotation_offset = 20

    def __init__(self, image, pos, rotation, direction):
        self.image = image
        self.pos = pos
        self.rotation = rotation
        self.direction = direction
